{
    "name": "slate",
    "private": true,
    "version": "1.0.0",
    "description": "Local network remote control application for PC",
    "scripts": {
        "setup": "npm install && cd server && pip install -r requirements.txt && cd ../client && npm install && cd ../editor && npm install",
        "dev": "concurrently --raw --kill-others-on-fail --names \"server,client\" \"npm run dev:server\" \"npm run dev:client\"",
        "dev:server": "cd server && python main.py",
        "dev:client": "cd client && npm run dev -- --host",
        "dev:editor": "cd editor && npm run tauri dev",
        "editor": "cd editor && npm run tauri dev"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "license": "GPL-3.0"
}